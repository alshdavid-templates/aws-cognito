{
  "scripts": {
    "build": "npx esbuild --bundle --platform=node --outfile=./dist/function.js ./server/main_lambda.mjs && cp -r ./client ./dist"
  },
  "dependencies": {
    "aws-jwt-verify": "latest"
  },
  "devDependencies": {
    "esbuild": "latest"
  }
}
